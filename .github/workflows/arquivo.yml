name: Exemplo de Leitura de Arquivo

on:
    workflow_dispatch:


jobs:
  read-file:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

#      - name: List files
#        run: ls -la
        
#      - name: Read env file
#        run: |
#          conteudo=$(cat .github/workflows/action-config.env)
#          echo "$conteudo"
      - name: Read env file and set ACTION variable
        id: setvar
        run: |
          echo "ACTION=$(grep 'ACTION_TYPE' action-config.env | cut -d'=' -f2)" >> $GITHUB_ENV
          
#      - name: Rodar se for apply
#        if: '$ACTION == "apply"'
#        run: |
#          echo "aqui se for apply"